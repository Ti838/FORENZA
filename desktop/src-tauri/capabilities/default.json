{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for FORENZA desktop client",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
